# Sensitive values. Copy to .env.secrets and keep it out of VCS.

# Do NOT commit .env.secrets

APP_KEY=base64:CHANGEME_BASE64_KEY

# Database
DB_PASSWORD=change_me

# Optional: set only if configuring Redis auth
# REDIS_PASSWORD=change_me_redis

